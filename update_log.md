# 更新日志

## 2026/5/8
- **解决**
加入了偏航角环，使得外环方向环的输出作为偏航角环的设定值，偏航角环的输出最终作用在左右轮的速度设定上，最终速度环输出到最终的电机pwm

## 2026/5/9 - 2026/5/10
- **问题**
在PID内环外环联调测试的时候电机突然发生了疯转 ， 导致左电机损坏 ，初步排查应该是在LCD函数中也调用了方向环为了显示方向环的输出值 ，导致和控制算法发生了冲突，外环参数混乱导致最终输出到电机的PWM过高
- **解决**
通过参数的测试和LCD显示发现了问题，我将控制算法中的值做了可读处理，等电机到了应该不会再出现这种问题，然后加入了IMU和负压风扇，负压风扇只是初步程序，目前是可用的，但是这两个部分还没做代码分层

## 2026/5/16-2026/5/18
- **解决** 
重组车模完成了新电机检测，没有问题。
之后做了速度环的PID参数整定，得到最优参数Kp = 1.2，Ki = 0.2
在代码中新加了vofa+的通讯模块用于PID调参
接下来任务是调方向环

## 2026/5/19-2026/5/20
- **解决**
1. 完成了方向环的第一次调参，初始速度100   PD参数整定过后能够完成地面循迹（除了苯环）
2. 加入了赛道保护模块，在检测不到电感时强制输出0占空比，并将检测是否停止标志位做了lcd显示
3. 加入了速度环限幅函数，使得增量式PID输出值不过大
4. 修复增量式PID内部致命错误，避免了积分饱和 ：（，这个问题困扰了好久

## 2026/5/24
- **解决**
1. 解决龙邱底层未做电源ADC口初始化问题
2. 加入了电源电压保护模块
3. 加入差速算法可变系数，使转向时内轮减速多一点，外轮加速少一点

## 2026/5/25-2026/5/27
- **解决**
1. 加入了负压模块，调试完毕，需要注意负压控制的占空比最好逐步控制，一次给太大的阶跃响应速度很慢而且伤害负压模块，经过测试发现车静止时负压在1340-1400左右可以完成上墙（1340）和倒挂圆筒（1360或者1400）较为保险
2. 加入了发车模块，按下*GO*按键1s以后发车
3. 优化了停车标志位*stop_flag*的控制
4. 修改了控制算法中的带宽，内环刷新频率为2ms一次，外环刷新频率为6ms一次  比例为 3：1

## 2026/5/28
- **解决**
1. 赛道已铺好，并且确保通路
2. 完成自制电感测量与归一化
3. 完善了发车代码，并且增加了按键调PID模块，优化显示模块代码
4. 增加了发车后按下GO停止系统的代码，增加可控范围
5. 增加了角速度环，基本整定好了角速度环的PD，完成了对控制算法的初步优化